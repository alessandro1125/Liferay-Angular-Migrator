Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/modules/es7.object.get-own-property-descriptors', ['module', 'exports', 'require', './_export', './_own-keys', './_to-iobject', './_object-gopd', './_create-property'], function (module, exports, require) {
  var define = undefined;
  // https://github.com/tc39/proposal-object-getownpropertydescriptors
  var $export = require('./_export');
  var ownKeys = require('./_own-keys');
  var toIObject = require('./_to-iobject');
  var gOPD = require('./_object-gopd');
  var createProperty = require('./_create-property');

  $export($export.S, 'Object', {
    getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
      var O = toIObject(object);
      var getDesc = gOPD.f;
      var keys = ownKeys(O);
      var result = {};
      var i = 0;
      var key, desc;
      while (keys.length > i) {
        desc = getDesc(O, key = keys[i++]);
        if (desc !== undefined) createProperty(result, key, desc);
      }
      return result;
    }
  });
});
//# sourceMappingURL=es7.object.get-own-property-descriptors.js.map