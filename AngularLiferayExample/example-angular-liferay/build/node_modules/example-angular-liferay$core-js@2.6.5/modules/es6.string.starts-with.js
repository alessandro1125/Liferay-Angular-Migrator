// 21.1.3.18 String.prototype.startsWith(searchString [, position ])
'use strict';

Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/modules/es6.string.starts-with', ['module', 'exports', 'require', './_export', './_to-length', './_string-context', './_fails-is-regexp'], function (module, exports, require) {
  var define = undefined;
  var $export = require('./_export');
  var toLength = require('./_to-length');
  var context = require('./_string-context');
  var STARTS_WITH = 'startsWith';
  var $startsWith = ''[STARTS_WITH];

  $export($export.P + $export.F * require('./_fails-is-regexp')(STARTS_WITH), 'String', {
    startsWith: function startsWith(searchString /* , position = 0 */) {
      var that = context(this, searchString, STARTS_WITH);
      var index = toLength(Math.min(arguments.length > 1 ? arguments[1] : undefined, that.length));
      var search = String(searchString);
      return $startsWith ? $startsWith.call(that, search, index) : that.slice(index, index + search.length) === search;
    }
  });
});
//# sourceMappingURL=es6.string.starts-with.js.map