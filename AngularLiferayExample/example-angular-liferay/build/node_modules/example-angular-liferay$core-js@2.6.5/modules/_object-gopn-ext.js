Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/modules/_object-gopn-ext', ['module', 'exports', 'require', './_to-iobject', './_object-gopn'], function (module, exports, require) {
  var define = undefined;
  // fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
  var toIObject = require('./_to-iobject');
  var gOPN = require('./_object-gopn').f;
  var toString = {}.toString;

  var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames ? Object.getOwnPropertyNames(window) : [];

  var getWindowNames = function (it) {
    try {
      return gOPN(it);
    } catch (e) {
      return windowNames.slice();
    }
  };

  module.exports.f = function getOwnPropertyNames(it) {
    return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : gOPN(toIObject(it));
  };
});
//# sourceMappingURL=_object-gopn-ext.js.map