'use strict';

Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/library/modules/_string-repeat', ['module', 'exports', 'require', './_to-integer', './_defined'], function (module, exports, require) {
  var define = undefined;
  var toInteger = require('./_to-integer');
  var defined = require('./_defined');

  module.exports = function repeat(count) {
    var str = String(defined(this));
    var res = '';
    var n = toInteger(count);
    if (n < 0 || n == Infinity) throw RangeError("Count can't be negative");
    for (; n > 0; (n >>>= 1) && (str += str)) if (n & 1) res += str;
    return res;
  };
});
//# sourceMappingURL=_string-repeat.js.map