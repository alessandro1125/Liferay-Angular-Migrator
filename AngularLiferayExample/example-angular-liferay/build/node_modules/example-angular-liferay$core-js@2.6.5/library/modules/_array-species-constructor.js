Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/library/modules/_array-species-constructor', ['module', 'exports', 'require', './_is-object', './_is-array', './_wks'], function (module, exports, require) {
  var define = undefined;
  var isObject = require('./_is-object');
  var isArray = require('./_is-array');
  var SPECIES = require('./_wks')('species');

  module.exports = function (original) {
    var C;
    if (isArray(original)) {
      C = original.constructor;
      // cross-realm fallback
      if (typeof C == 'function' && (C === Array || isArray(C.prototype))) C = undefined;
      if (isObject(C)) {
        C = C[SPECIES];
        if (C === null) C = undefined;
      }
    }return C === undefined ? Array : C;
  };
});
//# sourceMappingURL=_array-species-constructor.js.map