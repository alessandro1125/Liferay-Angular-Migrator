Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/library/modules/_parse-int', ['module', 'exports', 'require', './_global', './_string-trim', './_string-ws'], function (module, exports, require) {
  var define = undefined;
  var $parseInt = require('./_global').parseInt;
  var $trim = require('./_string-trim').trim;
  var ws = require('./_string-ws');
  var hex = /^[-+]?0[xX]/;

  module.exports = $parseInt(ws + '08') !== 8 || $parseInt(ws + '0x16') !== 22 ? function parseInt(str, radix) {
    var string = $trim(String(str), 3);
    return $parseInt(string, radix >>> 0 || (hex.test(string) ? 16 : 10));
  } : $parseInt;
});
//# sourceMappingURL=_parse-int.js.map