'use strict';
// 26.1.5 Reflect.enumerate(target)

Liferay.Loader.define('example-angular-liferay$core-js@2.6.5/library/modules/es6.reflect.enumerate', ['module', 'exports', 'require', './_export', './_an-object', './_iter-create'], function (module, exports, require) {
  var define = undefined;
  var $export = require('./_export');
  var anObject = require('./_an-object');
  var Enumerate = function (iterated) {
    this._t = anObject(iterated); // target
    this._i = 0; // next index
    var keys = this._k = []; // keys
    var key;
    for (key in iterated) keys.push(key);
  };
  require('./_iter-create')(Enumerate, 'Object', function () {
    var that = this;
    var keys = that._k;
    var key;
    do {
      if (that._i >= keys.length) return { value: undefined, done: true };
    } while (!((key = keys[that._i++]) in that._t));
    return { value: key, done: false };
  });

  $export($export.S, 'Reflect', {
    enumerate: function enumerate(target) {
      return new Enumerate(target);
    }
  });
});
//# sourceMappingURL=es6.reflect.enumerate.js.map